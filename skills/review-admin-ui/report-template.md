# Admin UI Review — <RUN_ID>

> Generated by the `review-admin-ui` skill. **Run folder:** this directory
> (`.tracking/ui-reviews/<RUN_ID>/`). Stack: frontend `http://127.0.0.1:<FPORT>`,
> backend `http://127.0.0.1:<BPORT>`, DB `<prefix>_test_review` (EDU_TEST_MODE=1).
> Matrix: `docs/qa/admin-ui-checklist/`. **Proof of work:** every function in §2
> carries a screenshot in `./screenshots/`.
>
> **This report is not done until the genuine-work gate passes:**
> `python scripts/verify_review_report.py <this-file> --require-screenshots`
> (every checklist function must have a verdict + proof). Hand it straight to
> Claude Code — §3 is a top-down, implementable fix backlog.

## How to use this report (for the implementer)
Work §3 **Fix backlog** top-to-bottom. Each item names the function key `[NN-F#]`,
the failing gate, observed/expected, the component file to edit, a concrete fix,
and its screenshot. Re-run the skill after a batch to confirm gates flip to PASS.

---

## 1. Summary

| File (tab) | Functions | ✅ PASS | ❌ FAIL | ⚪ NOT-OBSERVED |
|------------|-----------|--------|--------|----------------|
| 00 shell/login | 9 | | | |
| 01 dashboard | 12 | | | |
| … | | | | |
| **Total** | **187** | | | |

Environment notes (seed used, console-error pages, anything that blocked a check):
- …

---

## 2. Coverage — the genuine-work record (ONE line per function; ALL 187 required)

**Format (machine-checked by `verify_review_report.py`):**
```
- `[NN-F#]` VERDICT — `screenshots/<file>.png`[, …] — note
```
- **VERDICT** ∈ `PASS` · `FAIL` · `PARTIAL` · `NOT-OBSERVED` · `BLOCKED`.
- `PASS`/`FAIL`/`PARTIAL` **must** carry ≥1 screenshot (proof you saw it).
- `NOT-OBSERVED`/`BLOCKED` **must** carry a short reason (a state you couldn't reach).
- Screenshot naming: `<NN>-F<n>-<lang>-<viewport>.png` (e.g. `08-F2-en-desktop.png`);
  capture the EN/中文 × phone/desktop cells you actually exercised.
- Every `[NN-F#]` from every `docs/qa/admin-ui-checklist/NN-*.md` MUST appear here.

### 00 — Global shell & login
- `[00-F1]` PASS — `screenshots/00-F1-en-desktop.png`, `screenshots/00-F1-zh-phone.png` — sign-in worked; landed on dashboard.
- `[00-F2]` …
<!-- …one line per function, F1..F9… -->

### 01 — Dashboard
- `[01-F1]` …
<!-- …continue for every file 00–19, every function… -->

---

## 3. Fix backlog (priority-ordered — implement top-down)

Severity: **P1** = blocks a non-tech tester / data-loss / broken page · **P2** =
gate fails but workable · **P3** = polish.

### P1
1. **`[08-F2]` · 08-exams · Advanced search — Q3**
   - **Observed:** had to type the number `1000` to filter by status (EN, phone).
   - **Expected:** a dropdown of plain status names.
   - **Likely code:** `src/frontend/admin/src/app/[locale]/exams/_search-tokens.ts`
   - **Fix:** replace the freeform `state:` token with a `<Select>` of the status labels; map label→code in the query builder.
   - **Proof:** `screenshots/08-F2-en-phone.png`

### P2
…

### P3
…

---

## 4. Machine-readable findings (optional, for tooling)

```json
[
  {
    "key": "08-F2", "tab": "08-exams", "gate": "Q3", "verdict": "FAIL",
    "severity": "P1", "lang": "en", "viewport": "phone",
    "observed": "status filter requires typing 1000",
    "expected": "dropdown of plain status names",
    "likely_code": "src/frontend/admin/src/app/[locale]/exams/_search-tokens.ts",
    "fix": "replace freeform state token with a Select of status labels",
    "screenshot": "screenshots/08-F2-en-phone.png"
  }
]
```
