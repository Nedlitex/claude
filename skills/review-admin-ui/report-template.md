# Admin UI Review — <DATE>

> Generated by the `review-admin-ui` skill. Stack: frontend `http://127.0.0.1:<FPORT>`,
> backend `http://127.0.0.1:<BPORT>`, DB `<prefix>_test_review` (EDU_TEST_MODE=1, dedicated/disposable). Matrix:
> `docs/qa/admin-ui-checklist/`. **This report is written to be handed straight to
> Claude Code: each finding names the likely component and a concrete fix, ordered
> so an implementer can work top-down.**

## How to use this report (for the implementer)
Work the **Fix backlog** top-to-bottom. Each item is self-contained: tab + function
+ failing gate + observed/expected + the component file to edit + a suggested fix.
Re-run the `review-admin-ui` skill after a batch to confirm the gate flips to PASS.

---

## 1. Summary

| File (tab) | Functions | ✅ pass | ❌ fail | ⚪ not-observed |
|------------|-----------|--------|--------|----------------|
| 00 shell/login | | | | |
| 01 dashboard | | | | |
| … | | | | |
| **Total** | | | | |

Environment notes (seed data created, anything that blocked a check, console-error
pages, etc.):
- …

---

## 2. Fix backlog (priority-ordered — implement top-down)

Severity: **P1** = blocks a non-tech tester / data-loss / broken page · **P2** =
gate fails but workable · **P3** = polish.

### P1
1. **<tab> · F# · <function> — Q# <gate>**
   - **Observed:** <what actually happened, which lang/viewport>
   - **Expected:** <what the gate requires>
   - **Likely code:** `src/frontend/admin/...` (from the checklist 🚩 watch-point or the page snapshot)
   - **Fix:** <concrete change — e.g. "replace the freeform `state:` token with a `<Select>` of the 6 plain status labels; map label→code in the query builder">
   - **Proof:** `screenshots/mcp/review/<file>.png`

### P2
…

### P3
…

---

## 3. Full per-function results

For traceability — every function block walked, with the gate ticks as run.

### 00 — Global shell & login
- **F1 Sign in with .pem** — EN/desktop ✅ · EN/phone ✅ · 中文/desktop ✅ · 中文/phone ✅
  - notes: …
- **F2 …**

### 01 — Dashboard
- **F1 …**

<!-- …one section per checklist file… -->

---

## 4. Machine-readable findings (optional, for tooling)

```json
[
  {
    "tab": "08-exams",
    "function": "F2",
    "gate": "Q3",
    "severity": "P1",
    "lang": "en",
    "viewport": "phone",
    "observed": "status filter requires typing the number 1000",
    "expected": "dropdown of plain status names",
    "likely_code": "src/frontend/admin/src/app/[locale]/exams/_search-tokens.ts",
    "fix": "replace freeform state token with a Select of the status labels",
    "screenshot": "screenshots/mcp/review/08-exams-state-filter.png"
  }
]
```
